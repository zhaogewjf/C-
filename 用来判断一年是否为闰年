#define  _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>

int runnian(int x)
{
	if ( x % 400 ==0 || (x%4 == 0 && x%100 == 0))
	{
		return 1;
	}
	else
	{
		return 0;
	}
}
int main()
{
	int num = 0;
	num = runnian(2004);
	printf("%d\n",num);
	return 0;
}
